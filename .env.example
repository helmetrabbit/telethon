# ── Postgres (REMOTE default: OpenClaw server over Tailscale) ─────────
# Requires Tailscale connected and MagicDNS working (host "litterbox"),
# or replace host with the Tailnet IP (e.g. 100.110.29.6).
DATABASE_URL=postgresql://tgprofile:localdev@litterbox:5433/tgprofile?sslmode=disable

# ── Postgres (LOCAL docker-compose option) ────────────────────────────
# If you want to run local Postgres via `docker compose up -d postgres`,
# set DATABASE_URL to localhost and keep DATABASE_URL_DOCKER as-is.
# DATABASE_URL=postgresql://tgprofile:localdev@localhost:5432/tgprofile?sslmode=disable
DATABASE_URL_DOCKER=postgresql://tgprofile:localdev@postgres:5432/tgprofile?sslmode=disable

# ── Credentials (used by docker-compose for local Postgres only) ──────
POSTGRES_USER=tgprofile
POSTGRES_PASSWORD=localdev
POSTGRES_DB=tgprofile
POSTGRES_PORT=5432

# ── LLM (optional) ───────────────────────────────────────────────────
# OPENROUTER_API_KEY=
# DM profile extraction during DM ingest:
#   auto  -> enable when OPENROUTER_API_KEY is set
#   1/true -> force on
#   0/false -> force off
# DM_PROFILE_LLM_EXTRACTION=auto
# DM_PROFILE_LLM_MODEL=deepseek/deepseek-chat

# DM responder conversational model:
# DM_RESPONSE_LLM_ENABLED=1
# DM_RESPONSE_MODEL=deepseek/deepseek-chat
# DM_RESPONSE_MAX_TOKENS=300
# DM_RESPONSE_TEMPERATURE=0.2
